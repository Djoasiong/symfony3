<main class="container-fluid text-dark">
	{{ form_start(form) }}
	<div class="row fs-5">
		{{ form_widget(form) }}
	</div>
	<div class="d-flex justify-content-center mt-5 mb-5">
		<button class="btn btn-secondary p-2 px-4 mb-2">{{ button_label|default('Ajouter') }}</button>
	</div>
	{{ form_end(form) }}
</main>