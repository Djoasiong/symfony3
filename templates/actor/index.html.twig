{% extends 'base.html.twig' %}

{% block title %}All actors{% endblock %}

{% block body %}
	<h1>Tous les acteurs:</h1>
	{% for actor in actors %}
		<div>
			<h2>{{ loop.index }} / {{ actor.name }}<a href= "{{ path("actor_show", { id: actor.id }) }}">{{ actor.name }}</a></h2>
		</div>
		{% else %}Aucun acteur trouvé.
	{% endfor %}
	<a href="{{ path('program_index') }}">Retour aux programmes</a>
{% endblock %}