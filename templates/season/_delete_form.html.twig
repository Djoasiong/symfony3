<form method="post" action="{{ path('season_delete', {'id': season.id}) }}" onsubmit="return confirm({{ 'deleteItem'|trans }});">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ season.id) }}">
    <button class="btn">{{ 'delete'|trans }}</button>
</form>
